export enum GroupCourseType {
  ALL_SELECTION = "ALL_SELECTION",
  SINGLE_SELECT = "SINGLE_SELECT",
  MULTIPLE_SELECTION = "MULTIPLE_SELECTION",
}

export enum PLanStatus {
  DRAFT = "DRAFT",
  COMPLETED = "COMPLETED",
}

export enum LetterGrade {
  F = 'F',
  D = 'D',
  D_PLUS = 'D+',
  C = 'C',
  C_PLUS = 'C+',
  B = 'B',
  B_PLUS = 'B+',
  A = 'A',
  A_PLUS = 'A+',
}
